# 数据库迁移

当前发布只更新分析服务，不改变数据库。历史编号偏移只属于原正式库，不能复制到新库。
新测试库使用 scripts/init-test-teable.mjs 原生自动编号并创建 5 个视图；重复运行保留已有记录。
未来迁移须独立编号，声明旧版本条件，先备份，在测试库验证幂等、并发编号及回退方式，再人工批准执行。部署发现 automaticMigrations 非空即停止。不能用旧数据库备份无条件覆盖发布后的新记录。
