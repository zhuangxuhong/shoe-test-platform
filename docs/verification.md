# 本地验证记录

- 示例模式 18 条虚构记录，网页柱状图及 6 条雷达图渲染正常，浏览器无错误。
- 10 项自动测试通过：排名、缺失值、选择上限、雷达得分、只读接口、静态目录边界、Teable 1002 条分页读取与配置验证。
- 独立 Docker 项目 shoe-team-dev（本机 13001）：账号、表、18 条样例、自动编号、5 个视图初始化成功。连续两次初始化未重复导入。只读令牌用于本机 8092 联调成功。
- 新测试库实际保存两条仅填写日期/名称的记录，自动编号 T0019、T0020，互不重复；随后仅删除这两条测试记录，18 条样例保留。
- 发布脚本在 shoe-release-verify 临时容器、18089 端口完成构建、旧镜像导出、切换、健康和数据校验；随后回退成功。
- 正式 8088 服务没有部署此协作基线，原有生产容器镜像保持不变。
- GitHub 私有远端已建立，主分支与首个 PR 的 GitHub Actions 检查通过；CODEOWNERS 已配置为 @zhuangxuhong。
- 当前套餐对私有仓库分支保护返回 HTTP 403，强制审批尚未启用。同账号不能批准自己的 PR，双人审批需要真实团队成员参与验证。

验证完整联调可运行 node scripts/verify-test-teable.mjs。此脚本只访问本机 13001 的测试库；会短暂添加并删除自身的两条验证记录，自增序号会前移，留下空号是正常行为。
